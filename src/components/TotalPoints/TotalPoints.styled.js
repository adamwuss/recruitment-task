import styled from "styled-components";

const Text = styled.div`
  font-size: 15px;
  margin-bottom: 15px;
`;

const TotalPoints = styled.span`
  font-weight: 700;
`;
export default {
  Text,
  TotalPoints,
};
